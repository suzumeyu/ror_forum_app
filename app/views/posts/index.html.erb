<h1>Welcome on computer forum!</h1>
<h2>Place for everyone - from newcomers to computer geeks!</h2>

<div id="posts">
    <% @posts.each do |post| %>
        <div>
            <h3><%= post.title %></h3>
            <%= truncate(post.content, length: 150, separator: ' ') %>
        </div>
    <% end %>
</div>