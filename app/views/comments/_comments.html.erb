<h3 class="comments-title">
  Comments (<%= comments.count %>)
</h3>

<% if comments.empty? %>
  <p class="no-comments">No comments? Be the first one!</p>
<% else %>
  <div class="comments-list">
    <% comments.each do |comment| %>
      <%= render comment %>
    <% end %>
  </div>
<% end %>