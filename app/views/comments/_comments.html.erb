<h3>Comments (<%= comments.count %>)</h3>

<% if comments.empty? %>
    <p> No comments? Be the first one!</p>
<% else %>
    <% comments.each do |comment| %>
        <%= render comment %>
    <% end %>
<% end %>